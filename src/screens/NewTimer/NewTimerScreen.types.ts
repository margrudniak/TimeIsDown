import {StackScreenProps} from '@react-navigation/stack';
import {RootParamList, Screens} from 'src/navigation/screen-params';

export interface NewTimerProps
  extends StackScreenProps<RootParamList, Screens.NewTimer> {}
